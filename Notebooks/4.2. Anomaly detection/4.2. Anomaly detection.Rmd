---
title: "4.2. Anomaly detection"
output: html_notebook
---

Ucitavanje potrebnih biblioteka:
```{r}
library(reshape2)
library(patchwork)
```

## Univarijantna analiza netipicnih vrednosti

### Numericki atributi

#### Age

Prikaz varijable Age pomocu boxplot-a:
```{r}
ggplot(data, aes(x = Age)) +
  geom_boxplot() +
  theme_minimal() +
  labs(title = "Boxplot za Age", x = "Age")
```

#### Annual.Income

Prikaz varijable Annual.Income pomocu boxplot-a:
```{r}
ggplot(data, aes(x = Annual.Income))  +
  geom_boxplot() +
  theme_minimal() +
  labs(title = "Boxplot za Annual.Income", x = "Annual.Income")
```
#### Health.Score

Prikaz varijable Health.Score pomocu boxplot-a:
```{r}
ggplot(data, aes(x = Health.Score))  +
  geom_boxplot() +
  theme_minimal() +
  labs(title = "Boxplot za Health.Score", x = "Health.Score")
```

#### Vehicle.Age

Prikaz varijable Vehicle.Age pomocu boxplot-a:
```{r}
ggplot(data, aes(x = Vehicle.Age))  +
  geom_boxplot() +
  theme_minimal() +
  labs(title = "Boxplot za Vehicle.Age", x = "Vehicle.Age")
```

#### Credit.Score

Prikaz varijable Credit.Score pomocu boxplot-a:
```{r}
ggplot(data, aes(x = Credit.Score))  +
  geom_boxplot() +
  theme_minimal() +
  labs(title = "Boxplot za Credit.Score", x = "Credit.Score")
```

#### Premium.Amount

Prikaz varijable Premium.Amount pomocu boxplot-a:
```{r}
ggplot(data, aes(x = Premium.Amount))  +
  geom_boxplot() +
  theme_minimal() +
  labs(title = "Boxplot za Premium.Amount", x = "Premium.Amount")
```


### Kategorijski atributi

#### Gender

Graficka reprezentacija podataka:
```{r}
ggplot(data, aes(x = Gender, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Procentualna zastupljenost polova", x = "Gender", y = "Procenat") +
  theme_minimal()
```

#### Marital.Status

Graficka reprezentacija podataka
```{r}
ggplot(data, aes(x = Marital.Status, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Marital.Status", x = "Marital.Status", y = "Procenat") +
  theme_minimal()
```


#### Education.Level

Graficka reprezentacija podataka
```{r}
ggplot(data, aes(x = Education.Level, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Education.Level", x = "Education.Level", y = "Procenat") +
  theme_minimal()
```

#### Occupation

Graficka reprezentacija podataka
```{r}
ggplot(data, aes(x = Occupation, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Occupation", x = "Occupation", y = "Procenat") +
  theme_minimal()
```

#### Location

Graficka reprezentacija podataka:
```{r}
ggplot(data, aes(x = Location, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Location", x = "Location", y = "Procenat") +
  theme_minimal()
```

#### Policy.Type

Graficka reprezentacija podataka:
```{r}
ggplot(data, aes(x = Policy.Type, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Policy.Type", x = "Policy.Type", y = "Procenat") +
  theme_minimal()
```

#### Customer.Feedback

Graficka reprezentacija podataka:
```{r}
ggplot(data, aes(x = Customer.Feedback, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Customer.Feedback", x = "Customer.Feedback", y = "Procenat") +
  theme_minimal()
```

#### Smoking.Status

Graficka reprezentacija podataka:
```{r}
ggplot(data, aes(x = Smoking.Status, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Smoking.Status", x = "Smoking.Status", y = "Procenat") +
  theme_minimal()
```

#### Exercise.Frequency

Graficka reprezentacija podataka:
```{r}
ggplot(data, aes(x = Exercise.Frequency, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Exercise.Frequency", x = "Exercise.Frequency", y = "Procenat") +
  theme_minimal()
```

#### Property.Type

Graficka reprezentacija podataka:
```{r}
ggplot(data, aes(x = Property.Type, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Property.Type", x = "Property.Type", y = "Procenat") +
  theme_minimal()
```

#### Number.of.Dependents

Graficka reprezentacija podataka: 
```{r}
ggplot(data, aes(x = Number.of.Dependents, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Number.of.Dependents", x = "Number.of.Dependents", y = "Procenat") +
  theme_minimal()
```

#### Previous.Claims

Graficka reprezentacija podataka:
```{r}
ggplot(data, aes(x = Previous.Claims, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Previous.Claims", x = "Previous.Claims", y = "Procenat") +
  theme_minimal()
```


#### Insurance.Duration

Graficka reprezentacija podataka:
```{r}
ggplot(data, aes(x = Insurance.Duration, y = ..prop.., group = 1)) +
  geom_bar(fill = "thistle", color = "black") +
  geom_text(aes(label = scales::percent(..prop..)), stat = "count", vjust = -0.5) +
  labs(title = "Bar plot za Insurance.Duration", x = "Insurance.Duration", y = "Procenat") +
  theme_minimal()
```

### Zakljucak
- Iako Annual.Income i Premium.Amount imaju pozitivno asimetricnu distribuciju sa malim brojem observacija gde je iznos primanja, odnosno premije, veliki, zakljucujemo da je to normalna pojava - oznacene vrednosti na boxplot-u ne predstavljaju anomalije.
- Medju kategorijskim atributima nije uocen nijedan slucaj gde postoje netipicne vrednosti.